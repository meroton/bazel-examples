load(":lint.bzl", "lint", "lint_inline")
load("@rules_python//python:defs.bzl", "py_library")

lint(
    name = "no",
    should_lint = False,
)

py_library(
    name = "broken-formatting",
    srcs = ["exemplar/ugly.py"],
    aspect_hints = [
        ":no",
    ],
)

py_library(
    name = "ok-formatting",
    srcs = ["exemplar/ok.py"],
    aspect_hints = [
        lint_inline(
            name = "yes",
            should_lint = True,
        )
    ],
)
